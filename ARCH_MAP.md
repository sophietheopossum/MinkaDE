I'm working on a high-performance, highly customizable Linux desktop environment built around ShojiWM (https://github.com/bea4dev/ShojiWM) for power users. They need a functional, visually distinct compositor pipeline and core layout. With that in mind:

### Core Architecture & Strategy
1. Fork ShojiWM and implement HDR support by extending its custom shader pipeline to flag HDR metadata (PQ/Rec.2020) and handle Wayland color management. All commits must match the existing coding style so they can be merged upstream.
2. Build the companion shell as a hybrid of two Wayland-native layer-shell runtimes, both peers on ShojiWM's existing NDJSON IPC socket: Quickshell (Qt6/QML) for widget- and service-heavy surfaces (bar, launcher, control center, notification daemon, system tray, lockscreen), and Guido (Rust + wgpu, https://malpenzibo.github.io/guido/) for latency-sensitive animated overlays (snap previews, drag feedback, OSDs) with spring physics and SDF rendering. All IPC strictly non-blocking; animate client-side toward broadcast state rather than streaming frames. shoji-bar-2 (https://github.com/bea4dev/shoji-bar-2) retired 8/7/2026: the ShojiWM config now spawns MinkaShell as the session shell and MinkaFX (with the MinkaIPC crate) owns the snap preview; the repo remains for historical reference only. Visual references: caelestia-shell (layout/structure), zephyr by flickowoa (animated elements).
3. Create a specialized dual-display mode tailored for the ASUS Zenbook Duo UX482. When active, all panels, system tray widgets (power, audio, notifications), and global menus must pin to the bottom secondary display (1920x515), leaving the main display entirely clear for application windows. Make sure there is a basic KDE-style layout available for other systems.

### Visual & Shader Constraints
* Maintain a high-contrast, black-and-red aesthetic inspired by the "Eternal Darkness" theme, using purple strictly as a subtle tertiary accent.
* The system must heavily leverage ShojiWM's fragment shaders to achieve two distinct effects: a "frosted glass" blur on window titlebars, and a reactive "liquid glass" physics distortion on transparent surfaces (similar to liquid-terminal-config-shojiwm).
* Prioritize raw rendering performance and animation fluidity over complex security sandboxing.

### Deferred / Later
HDR polish (working baseline shipped 2026-07: config `hdr: true` → PQ/BT.2020 signaling + fp16 encode pass; not urgent):
* ~~PQ client-content input transform: surfaces tagged PQ/BT.2020 are composited as if sRGB, so HDR video (mpv) looks washed out~~
Done 2026-07-30, confirmed against mpv. `clipped_surface.frag` converts tagged content into the compositing space on sample: PQ EOTF (inverse of `output_encode.frag`'s), BT.2020→BT.709 (inverse of its BT.2087 matrix), extended-Reinhard tone map, sRGB re-encode. Four `_1f` uniforms resolved once per element in `clipped_surface.rs` from `surface_image_description()` — which until now had **zero consumers**. `src_transfer == 0.0` is an early-out so untagged surfaces are unchanged. Note mode 3: sRGB transfer in BT.2020 primaries needs the gamut matrix but no transfer conversion, and would otherwise have wrongly taken the passthrough path. Alpha is unpremultiplied before the transfer and re-premultiplied after, or partially transparent edges halo.
  * `clipped_memory.frag` needs **nothing** — checked 30/7 after wrongly listing it as a gap. `ClippedMemoryElement` is used only by `icon.rs` and `text.rs`, i.e. MinkaDE's own rasterised icons and labels, which are always sRGB and can never carry a tag. Client SHM buffers are imported to GL textures by smithay and render through `WaylandSurfaceRenderElement` → `clipped_surface.frag` like dmabuf ones, so they were already covered.
  * **REACH IS NARROW — read this before assuming the conversion applies.** `clipped_surface.frag` only runs for elements wrapped in `ClippedSurfaceElement`, and `clipped_surface_elements` is only called with `Some(content_clip)` when a decoration yields `clips_surface`. Inside it, only the element matching `root_id` becomes `Clipped`; every subsurface, and *everything* when `clip` is `None`, becomes `WindowClipElement::Raw` and draws through smithay's default texture program. So the conversion currently covers **the root surface of windows with SSD content clipping, and nothing else** — windowed mpv works (it has decorations, hence a clip), fullscreen/undecorated windows and all subsurfaces get nothing. Fixing this is the same problem as phase 2, since that needs every element writing linear into the intermediate too; `GlesFrame::override_default_tex_program` (smithay `gles/mod.rs:2614`) is the likely mechanism for both.
  * Subsurface *lookup* fixed 30/7 (correct, but cannot take effect until the reach problem above is solved): descriptions are collected **per surface** into a `HashMap<Id, ImageDescription>` by walking the tree with `with_surface_tree_downward`, then paired to elements via `Element::id()` — which matches because `WaylandSurfaceRenderElement::from_surface` derives its id with `Id::from_wayland_resource(surface)`. Pairing by id deliberately avoids reimplementing smithay's tree walk, so its view-offset/positioning logic stays the single source of truth. The walk is gated on `color_management::any_surface_tagged()`, a sticky global set the first time anything is tagged, so untagged sessions pay one relaxed atomic load per window per frame.
  * Tone curve replaced 30/7: extended Reinhard → **ITU-R BT.2390 EETF**, per channel on a PQ signal. Below the knee it is the identity, so SDR content passes through *exactly* (verified numerically: 0.2→0.2, 20→20, 80→80 nits); above it, a Hermite spline rolls off to the target peak. Reinhard's flaw was compressing midtones alongside highlights and flattening the whole image. Knee parameters (`src_pq_lo`/`src_pq_hi`/`dst_pq_hi`) are PQ-encoded **on the CPU** via `color::colorimetry::pq_inverse_eotf`, since they are constant per surface and would otherwise cost four pow-heavy calls per fragment.
  * Target peak is the content's own reference white, so it never clips. Reference white lands at ~0.85 relative for a 600-nit master, ~0.78 at 1000 nits, ~0.55 at 10000. That last figure only occurs when a client sends **no** luminances, since the protocol's PQ default assumes a 10000-nit master — `max_cll` is preferred where present. If it ever looks too dim, the knob is a headroom multiplier on `dst_pq_hi`: 1.5× lifts reference white to ~0.98 at 1000 nits but starts clipping above ~410.
  * **Pre-existing shader bug fixed 30/7, unrelated to colour:** `clipped_surface.frag` had **no `//_DEFINES_` marker**, so smithay's plain `src.replace` was a no-op and all three variants compiled with `EXTERNAL` and `NO_ALPHA` undefined — external-OES buffers were sampled through a `sampler2D` declaration, and `NO_ALPHA` was never honoured despite smithay documenting it as mandatory for custom texture shaders. Fixed, plus the GLSL ES ordering it exposed (`#extension` must precede `precision` and every declaration). `NO_ALPHA` forces alpha **immediately after the sample, never at the end** — at the end it would overwrite the `rounded_alpha` corner coverage and square off every corner. The marker's name must appear exactly once in the file: the replacement is global, so a second occurrence even in a comment injects a `#define` mid-line.
  * **Reach fix / phase 2 — scoped by a 9-agent adversarial review 30/7. Do not start this casually; three independent refuters returned "sound-with-fixes" with 7 fatal flaws against the first plan.**
    * There are **three** consumers of client-content elements, not one: `TtyRenderElements` (tty.rs:1471), `WinitRenderElements` (winit.rs:2025, with `WindowClipElement::Raw` destructured at winit.rs:1394/1493/4845), and `ToplevelCaptureElement` (image_copy_capture_render.rs:39) — the ext-image-copy-capture path feeding MinkaCap, fed from smithay's `AsRenderElements` and unreachable by any type change to the other two.
    * **The current implementation cannot reach real HDR by flipping a flag.** Two of the six uniforms encode the *destination*: `dst_pq_hi` is `to_pq(luminances.reference)` — the content's own reference white — and `src_ref_nits` normalises each surface by its own declared reference, so surfaces declaring different references are scaled inconsistently against each other. Phase 2 must change that normalisation, not add a `dst_space` switch.
    * Other verified phase-2 blockers: the fp16 intermediate receives the **entire** element list including error text, FPS overlay and cursor (tty.rs:5347-5392), so linear compositing is not scoped to client surfaces; snapshots are keyed globally by window id (state.rs:306), so "convert once at capture" is unsound across outputs with different colour setups; and `shader_effect.rs:1430` allocates the backdrop buffer as `Abgr8888`, which hard-blocks linear before any shader work.
    * **Rejected, with reasons:** a frame-wide `override_default_tex_program` — no caller ever holds a `GlesFrame` (`OutputDamageTracker::render_output` builds and finishes it privately), its uniforms are fixed per frame so per-surface descriptions can't vary, `clear_tex_program_override()` sets `None` rather than restoring so the first `ClippedSurfaceElement` drawn would wipe it for everything after, and it would double-convert snapshots, decoration text and backdrops. A synthesized no-op `ContentClip` — the rounded mask is applied outside the `rect_bounds_enabled` gate, UV-snap compensation is unconditional, the geometry is only computable *inside* `new`, and the element drops opaque regions and underlying storage, which would disable occlusion culling and direct scanout session-wide. Per-site conversion across ~25 production sites — guaranteed to drift.
    * A wrapper element with an untagged pass-through is **also** wrong as a stepping stone: under linear compositing every surface needs conversion, so the untagged fast path becomes dead code and the design is paid for twice.
  * **PLAN — staged, written 31/7 after the adversarial review. Read the rejections above before deviating.**
    * *Organising principle:* the conversion is **source → destination**, and when the two match it must be a **structural no-op that preserves direct scanout**. Today the destination is hardcoded (sRGB-encoded BT.709 normalised to the content's own reference), so a PQ client on a PQ output is still converted pointlessly. That matters because any element which must run a shader **cannot** be scanned out: `WaylandSurfaceRenderElement::underlying_storage` returns `Some(UnderlyingStorage::Wayland(..))` (smithay surface.rs:446) while `ClippedSurfaceElement` returns `None` (clipped_surface.rs:905) and empty `OpaqueRegions` (clipped_surface.rs:812) — which is exactly what `fullscreen_scanout_window` (tty.rs:606) exists to enable. So the end state is *not* "convert everything"; it is "convert only when the spaces differ", and the best case (PQ client on PQ output) becomes the fastest case.
    * **Stage 1 — fix the colour model, no coverage change.** Split the six uniforms into a source description and a *destination* description supplied by the output, instead of `dst_pq_hi = to_pq(content reference)` and per-surface `src_ref_nits` normalisation (clipped_surface.rs:555-568). Add an explicit "spaces match → passthrough" early-out. Contained to `clipped_surface.{rs,frag}`; regression test is that windowed mpv still looks right. Must come first: any coverage work built on the current model bakes in the SDR assumption and gets redone.
    * **Stage 2 — element property preservation.** Decide what a converting element reports for `opaque_regions` and `underlying_storage`. Tagged content genuinely cannot be scanned out, but it must still forward opaque regions or widening coverage disables occlusion culling session-wide. Untagged content must keep both. Prerequisite for stage 3.
    * **Stage 3 — widen coverage, all three consumers together.** `TtyRenderElements` (tty.rs:1471), `WinitRenderElements` (winit.rs:2025, `WindowClipElement::Raw` at :1394/:1493/:4845) and `ToplevelCaptureElement` (image_copy_capture_render.rs:39). Not separable — the winit one is a build break and the capture one is silent.
    * **Stage 4 — linear compositing.** Flip the destination space, drop `clamp(color.rgb, 0.0, 1.0)` in output_encode.frag, and audit *every* intermediate writer: the full element list including error text, FPS overlay and cursor (tty.rs:5347-5392), plus the `Abgr8888` backdrop buffer (shader_effect.rs:1430) which hard-blocks linear on its own.
    * **Stage 5 — snapshots.** Only after 1-4, because snapshots are keyed globally by window id (state.rs:306) and so cannot simply be converted at capture time when outputs differ.
    * *Unverified in this plan:* that a matched-space passthrough can actually retain scanout end-to-end through `DrmCompositor`'s plane assignment. Everything else above is verified against source.
  * **This is SDR-correct, not HDR.** `output_encode.frag` still does `clamp(color.rgb, 0.0, 1.0)` before its transfer, so HDR content is tone-mapped down rather than carried at HDR luminance. Real HDR needs linear compositing + dropping that clamp + converting every source on sample — the fp16 intermediate already has the headroom waiting.
* Promote `SHOJI_SDR_NITS` (SDR white level on the PQ signal, default 203) from env var into the runtime display config (`sdrNits` per output), same pattern as the `hdr` flag — env vars don't reach DM-launched sessions.
* Consider decoding the SDR composite with pure gamma 2.2 instead of the piecewise sRGB EOTF in `output_encode.frag` (KWin does this; avoids raised shadows / grayish blacks on HDR outputs).
* Send `image_description_changed`/`preferred_changed` to already-bound color-management clients on a live SDR↔HDR switch (needs per-object tracking in `protocols/color_management.rs`).
* ~~`create_windows_scrgb` is refused with `UnsupportedFeature` — the gamescope blocker (gamescope #1825, the same wall GNOME/mutter #4083 and Hyprland hit)~~
Done 2026-07-29: `create_windows_scrgb` now builds the protocol-fixed description — sRGB/BT.709 primaries, `ExtLinear` transfer, luminances stated explicitly as min 0 / max 10000 / reference 203 cd/m². The luminances *must* be explicit: `ExtLinear::default_luminances()` is the SDR 80 cd/m² set, which would clamp away the entire point of the encoding. `Feature::WindowsScrgb` is advertised alongside the other HDR entries, so it follows the same `hdr_experiment_enabled()` gate and still errors cleanly when HDR is off (which is what gamescope PR #1867's feature check reads). `ImageDescriptionData` gained `allow_information`, because the protocol forbids `get_information` on a Windows-scRGB description — it is usable but not introspectable. **Untested against real gamescope** (not installed here), and note this only fixes negotiation: per the first bullet above, extended-linear content is still composited as if sRGB until the per-surface input transform lands, so gamescope should stop erroring but may well still look wrong.
* ~~No `frog-color-management-v1` support — likely the Gamescope blocker~~
Checked 2026-07-29: not needed. Gamescope's Wayland backend moved to `wp_color_management_v1` in PR #1775 (merged 25/4/2025), which `protocols/color_management.rs` already implements; the frog protocol is Valve's pre-standardisation stopgap and nothing current reaches for it. The washed-out-Gamescope reports trace to scRGB above, not to frog.

X11 fractional-scaling rendering mode (baseline shipped 2026-07-10; X11 has one global DPI, so mixed-scale layouts force a tradeoff):
* Expose a user choice in MinkaConf between the two X11 bridge worlds:
* **World A "consistent size"** (current default: logical wl_output modes + `preferred_scale` pinned 1.0 for bridge clients → X apps sized like native apps on every screen, but upscale-soft on any output with scale > 1)
* **World B "crisp"** (stock xwayland-satellite device-pixel behavior → pixel-perfect on every screen, but UI size only correct on outputs matching the global Xft.DPI; optionally expose the DPI knob to pick which screen looks right).
* Plumbing:
* World A is `normalize_to_logical` (smithay fork ClientOutputOverride) + `preferred_scale_for_surface` pin (ShojiWM presentation.rs) — both would become a runtime setting instead of hardcoded.
* World B additionally requires xwayland-satellite ≥ upstream 33c344f (origin normalization) and the single-stable-output enter gating (refresh_space_outputs), which stays on in both worlds.
* ~~MinkaConf should normalize saved display layouts so the bounding box origin is (0,0) (like xrandr)~~
Done 2026-07-10, but in the TS runtime rather than MinkaConf: MinkaConf's commitDrag already normalized the arrangement it saves (connected displays only), which still let a disconnect strand the live subset off-origin. `output.configure` in index.tsx now re-anchors the connected, explicitly-positioned outputs to a (0,0) bounding box on every output change
— protects distro satellite < 33c344f and anything else assuming an origin-anchored screen.

CUDA one-shot image work in MinkaShot (idea only, nothing built, 2026-07-29):
* **Keep CUDA out of anything per-frame.** ShojiWM renders on the GPU that scans out (Intel), so a CUDA effect would round-trip Intel→NVIDIA→Intel every frame — ~2.8 ms for 1080p at gen 4 x4 plus sync — against blur/liquid-glass passes costing a fraction of a millisecond in GLES where the data already lives. That rules out the compositor, MinkaFX, and the Quickshell apps.
* The economics invert for one-shot work on a captured image: transfer once, compute hard, return something small. **OCR on a captured region** is the strongest candidate; subject/background segmentation (cut a window cleanly out of a shot) and upscaling a low-res capture are the same shape and could share the plumbing.
* Constraints: the MX450 has **2 GB VRAM**, so small models only — fine for OCR or segmentation, rules out anything general. No setup cost to prototype: the CUDA 13.3.1 toolkit is already installed and `__NV_PRIME_RENDER_OFFLOAD=1` is already system-wide in `/etc/environment`.
* Open: which OCR engine, and whether it earns a CUDA dependency at all over a CPU path that is merely slower — a screenshot is not a latency-critical operation.

Partition manager — foundation evaluated 31/7 (idea only, nothing built):
* **Build on util-linux, not on a Rust partitioning crate.** All of the following was verified by running it against an image file **as a normal user with no root**, which also means the whole thing is developable and testable without privileges or real disks.
  * `sfdisk --json` returns the full display model: `label`, `id`, `firstlba`, `lastlba`, `sectorsize`, and per-partition `node`/`start`/`size`/`type` GUID/`uuid`.
  * Resize works **and validates** — growing a partition into its neighbour's space was refused; shrinking succeeded. `-n` is a genuine dry run (exit 0, table verifiably unchanged) — a free preview/validate pass.
  * `--backup` writes one file per on-disk structure keyed by byte offset (protective MBR, both GPT headers, both entry arrays); restore is `dd` back to the offset. **Caution: without `-O <prefix>` it writes them into `$HOME`.**
  * `--move-data` relocates partition *data* and explicitly handles overlapping old/new locations — i.e. the crash-safe block copy that is otherwise multi-month work.
  * MBR + GPT + nested labels, `-N` for single-partition ops, `-w`/`-W` for controlled signature wiping.
  * `lsblk --json` gives device discovery plus `fstype`/`label`/`partuuid`/`parttypename`, also without root; `blkid` covers superblock probing.
  * This is essentially GParted's own architecture (GUI over external tools). Trade-off: JSON + exit codes + stderr strings instead of typed Rust APIs, and you inherit util-linux version differences.
* **`AerynOS/disks-rs` is the wrong tool for this, and its name misleads.** Verified: it does **not implement GPT** — `Cargo.toml:10` is `gpt = "4.0.0"` and `partitioning/src/lib.rs:15` is `pub use gpt;`, so all table codec work is the third-party `gpt` crate. Its planner's entire mutation vocabulary is two variants, `AddPartition` and `DeletePartition` (`partitioning/src/planner.rs:42-52`); a workspace-wide search finds **no** resize/shrink/grow/move function. Delete-then-re-add is not a shrink either — the writer zeroes the first 2 MiB of every added region, destroying the superblock you were preserving. And `Planner::new` models existing partitions from **sysfs** (number/start/size, `attributes: None`), never reading the real table, so it cannot populate a single column of a partition manager's main view. It is an *installer* planner: correct for a tool about to overwrite the disk, useless for an editor.
* Worth lifting from disks-rs anyway (MPL-2.0, file-level copyleft, so copying is workable): the **`superblock` crate** (ext4/btrfs/xfs/f2fs/FAT/LUKS2 probing, zerocopy, no unsafe, tested against seven real compressed fs images, and a dependency of nothing else so it lifts cleanly); `blkpg.rs` BLKPG ioctls; `formatter.rs` as a template for mkfs argv with per-filesystem quirks encoded; `loopback.rs`/`sparsefile.rs` as a destructive-test rig.
* Known bug if used: `plan_delete_partition` has no dedup, only a bounds check, so queueing the same index twice makes `current_layout` call `Vec::remove` twice — silently drops a neighbour, or panics on a single-partition disk. In a GUI where delete is a button, that is a crash path.
* Still yours regardless of foundation: **filesystem resize orchestration** (`resize2fs`/`xfs_growfs`/`btrfs filesystem resize`, the shrink-fs-before-partition ordering rules, and XFS being unable to shrink at all), and the GUI.

### Scope & Execution
**Anything given its own entry in this file is a future goal of MinkaDE, including applications.** "Skip generic utilities" below means don't invent scope — don't go off and write a text editor nobody asked for. It does not exclude the applications recorded here; those are deliberate and wanted, and the ordering note is about sequencing, not about whether they belong.

Focus entirely on the foundational architecture, the HDR shader pipeline, and the dual-screen display management code first. Skip generic utilities like text editors. You are fully empowered to choose the cleanest technical implementation for this desktop environment—if a specific shader approach or library works better than what I suggested, implement it and show me the results, this includes the window manager. 

Lead directly with the concrete architectural layout and the initialization code.
